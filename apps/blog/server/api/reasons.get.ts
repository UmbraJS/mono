// import type { Reason } from '../../types/reasoning'
// import data from '../../app/data'

export default defineEventHandler(() => {
  // Return the reasons array used by the blog components.
  // data.ts exports a default object: { user, reasons }
  // return data.reasons as Reason[]
  return []
})
