<template>
  <input class="button buttonText buttonHover buttonActive buttonFocus focus" />
</template>

<style lang="scss">
input {
  all: unset;
  background: var(--base);
  color: var(--base-contrast);
  padding: var(--space-1);
  border: solid var(--border-size) var(--base-60);
  border-radius: var(--radius);
}
</style>
